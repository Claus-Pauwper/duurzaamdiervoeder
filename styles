:root{
  --bg:#f7faf9;
  --card:#ffffff;
  --muted:#6b7280;
  --accent:#0f766e;
  --accent-2:#065f46;
  --danger:#dc2626;
  --radius:10px;
  --max-width:980px;
  font-family:Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
  color-scheme: light;
}

*{box-sizing:border-box}
html,body{height:100%}
body{
  margin:0;
  background:linear-gradient(180deg,var(--bg),#ffffff);
  color:#0f172a;
  -webkit-font-smoothing:antialiased;
  -moz-osx-font-smoothing:grayscale;
  line-height:1.45;
}

.container{
  max-width:var(--max-width);
  margin:0 auto;
  padding:24px;
}

.site-header{
  background:linear-gradient(90deg, rgba(15,118,110,0.95), rgba(6,95,70,0.95));
  color:white;
  padding:28px 0;
  border-bottom-left-radius:16px;
  border-bottom-right-radius:16px;
  margin-bottom:18px;
}
.site-header h1{margin:0;font-size:1.6rem;letter-spacing:0.2px}
.tagline{opacity:0.95;margin-top:6px;color:rgba(255,255,255,0.9)}

.intro p{margin-top:0;color:var(--muted)}

form{
  background:var(--card);
  border-radius:var(--radius);
  padding:18px;
  box-shadow:0 6px 18px rgba(15,23,42,0.06);
}

fieldset{
  border:1px solid #eef2f7;
  padding:12px;
  margin-bottom:12px;
  border-radius:8px;
}

legend{font-weight:600;color:var(--accent-2)}

label{
  display:block;
  margin-bottom:10px;
  font-size:0.95rem;
  color:#0f172a;
}

input[type="text"],
input[type="email"],
input[type="tel"],
input[type="number"],
select,
textarea{
  width:100%;
  padding:10px 12px;
  margin-top:6px;
  border:1px solid #e6eef2;
  border-radius:8px;
  background:#fff;
  font-size:0.95rem;
}

textarea{resize:vertical}

.actions{
  display:flex;
  gap:10px;
  flex-wrap:wrap;
  margin-top:8px;
}

button{
  background:var(--accent);
  color:white;
  border:0;
  padding:10px 14px;
  border-radius:8px;
  cursor:pointer;
  font-weight:600;
}

button#downloadBtn{background:#0ea5a4}
button#previewBtn{background:#2563eb}
button#sendBtn{background:var(--accent-2)}

button[disabled]{opacity:0.6;cursor:not-allowed}

.summary{
  margin-top:18px;
  background:#fff;
  padding:12px;
  border-radius:10px;
  box-shadow:0 6px 18px rgba(15,23,42,0.04);
}

.summary-pre{
  white-space:pre-wrap;
  background:#f8fafc;
  padding:12px;
  border-radius:8px;
  border:1px solid #eef2f7;
  max-height:360px;
  overflow:auto;
}

#formMessage{margin-top:10px;color:var(--muted)}

.site-footer{
  margin-top:28px;
  padding:18px 0;
  text-align:center;
  color:var(--muted);
  font-size:0.9rem;
}
@media(min-width:900px){
  form label{display:block;width:48%;float:left;margin-right:4%}
  form label:nth-child(2n){margin-right:0}
  fieldset{overflow:auto}
}
